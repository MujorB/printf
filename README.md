# The ALX **printf** Group Project  
To build your code, run `gcc *.c -o test`  
Then run `./test` to run it  
## man-page  
To view the man-page run `groff -man -Tascii ./man_3_printf | less`   
Having trouble exiting the manpage enter `q`
